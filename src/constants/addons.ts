/**
 * Slugs of the small upsell items shown in the cart's "Add a little
 * extra…" box. These reference real catalog products in products.ts so
 * the cart store, pricing, and stock rules stay unified across the app.
 */
export const ADDON_SLUGS: string[] = ["seed-starting-pots", "watering-can"];
